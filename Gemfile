source "https://rubygems.org"

gem "httparty"
# Pinned to the last release that ships a precompiled x64-mingw32 binary
# (Ruby 3.0 on Windows). 1.18+ only ships x64-mingw-ucrt and would build from source.
gem "nokogiri", "~> 1.17.2"
gem "svg_optimizer"
